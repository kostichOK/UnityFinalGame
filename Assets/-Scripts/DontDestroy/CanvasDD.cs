using UnityEngine;

public class CanvasDD : MonoBehaviour
{
    private static CanvasDD instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
