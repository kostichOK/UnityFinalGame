using UnityEngine;

public class GreenKDD : MonoBehaviour
{
    private static GreenKDD instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
