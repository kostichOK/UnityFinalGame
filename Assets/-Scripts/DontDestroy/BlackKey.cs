using UnityEngine;

public class BlackKey : MonoBehaviour
{
    private static BlackKey instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
