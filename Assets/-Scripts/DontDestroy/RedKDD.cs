using UnityEngine;

public class RedKDD : MonoBehaviour
{
    private static RedKDD instance;

    private void Awake()
    {
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
}
